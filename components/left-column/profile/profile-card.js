/**
 * Инициализация карточки профиля.
 * В текущей реализации не содержит логики, оставлена для совместимости.
 */
export function initProfileCard() {
  // На данный момент логика не требуется, оставляем пустую функцию
  // для сохранения структуры и совместимости с main-scripts.js
}
